{
  "name": "test-simple-react-admin",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "ra-input-rich-text": "^2.3.0",
    "react": "^16.5.1",
    "react-admin-firebase": "../",
    "react-dom": "^16.5.1",
    "react-scripts": "1.1.5",
    "redux-saga": "^0.16.0"
  },
  "scripts": {
    "start": "BROWSER=none PORT=3333 react-scripts start",
    "clean-dependencies": "yarn && yarn remove react-admin-firebase && yarn add ../ && yarn start",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
